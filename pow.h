#ifndef QUICK_POW
#define QUICK_POW

#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>

#define POW_FUNC_SIZE 9
uint64_t pow1(unsigned int, unsigned int);
uint64_t pow2(unsigned int, unsigned int);
uint64_t pow2_1(unsigned int, unsigned int);
uint64_t pow3(unsigned int, unsigned int);
uint64_t pow3_1(unsigned int, unsigned int);
uint64_t pow4(unsigned int, unsigned int);
uint64_t pow4_1(unsigned int, unsigned int);
uint64_t pow5(unsigned int, unsigned int);
uint64_t pow5_1(unsigned int, unsigned int);

#endif